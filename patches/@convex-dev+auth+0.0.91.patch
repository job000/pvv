diff --git a/node_modules/@convex-dev/auth/dist/nextjs/server/cookies.js b/node_modules/@convex-dev/auth/dist/nextjs/server/cookies.js
index 637917f..b8e61e6 100644
--- a/node_modules/@convex-dev/auth/dist/nextjs/server/cookies.js
+++ b/node_modules/@convex-dev/auth/dist/nextjs/server/cookies.js
@@ -2,12 +2,73 @@ import { cookies as nextCookies, headers as nextHeaders } from "next/headers";
 import * as utils from "../../server/utils.js";
 const cookies = nextCookies;
 const headers = nextHeaders;
+function parseAuthCookiesFromHeader(cookieHeader, host) {
+    const isLocalhost = utils.isLocalHost(host);
+    const prefix = isLocalhost ? "" : "__Host-";
+    const jwtName = prefix + "__convexAuthJWT";
+    const rtName = prefix + "__convexAuthRefreshToken";
+    const verName = prefix + "__convexAuthOAuthVerifier";
+    let token = null;
+    let refreshToken = null;
+    let verifier = null;
+    for (const part of cookieHeader.split(";")) {
+        const trimmed = part.trim();
+        const eq = trimmed.indexOf("=");
+        if (eq === -1)
+            continue;
+        const name = trimmed.slice(0, eq).trim();
+        let value = trimmed.slice(eq + 1).trim();
+        if (value.startsWith('"') && value.endsWith('"')) {
+            value = value.slice(1, -1);
+        }
+        if (name === jwtName)
+            token = value;
+        if (name === rtName)
+            refreshToken = value;
+        if (name === verName)
+            verifier = value;
+    }
+    return { token, refreshToken, verifier };
+}
 export async function getRequestCookies() {
     // maxAge doesn't matter for request cookies since they're only relevant for the
     // length of the request
-    return getCookieStore(await headers(), await cookies(), {
+    const hdrs = await headers();
+    const store = getCookieStore(hdrs, await cookies(), {
         maxAge: null,
     });
+    // Next.js 15+ can expose an empty `cookies()` in some RSC/document contexts while
+    // the Cookie header is still sent (e.g. opening `/` in a new tab).
+    const raw = hdrs.get("cookie");
+    if (!raw) {
+        return store;
+    }
+    const parsed = parseAuthCookiesFromHeader(raw, hdrs.get("Host") ?? "");
+    if (parsed.token === null &&
+        parsed.refreshToken === null &&
+        parsed.verifier === null) {
+        return store;
+    }
+    return {
+        get token() {
+            return store.token ?? parsed.token;
+        },
+        set token(value) {
+            store.token = value;
+        },
+        get refreshToken() {
+            return store.refreshToken ?? parsed.refreshToken;
+        },
+        set refreshToken(value) {
+            store.refreshToken = value;
+        },
+        get verifier() {
+            return store.verifier ?? parsed.verifier;
+        },
+        set verifier(value) {
+            store.verifier = value;
+        },
+    };
 }
 export async function getRequestCookiesInMiddleware(request) {
     // maxAge doesn't matter for request cookies since they're only relevant for the
diff --git a/node_modules/@convex-dev/auth/dist/nextjs/server/index.js b/node_modules/@convex-dev/auth/dist/nextjs/server/index.js
index a4bf0fc..1d62d5e 100644
--- a/node_modules/@convex-dev/auth/dist/nextjs/server/index.js
+++ b/node_modules/@convex-dev/auth/dist/nextjs/server/index.js
@@ -71,7 +71,7 @@ handler, options = {}) {
         let response = null;
         // Forward cookies to request for custom handler
         if (authResult.kind === "refreshTokens" &&
-            authResult.refreshTokens !== undefined) {
+            authResult.refreshTokens != null) {
             logVerbose(`Forwarding cookies to request`, verbose);
             await setAuthCookiesInMiddleware(request, authResult.refreshTokens);
         }
@@ -108,7 +108,7 @@ handler, options = {}) {
         }
         // Port the cookies from the auth middleware to the response
         if (authResult.kind === "refreshTokens" &&
-            authResult.refreshTokens !== undefined) {
+            authResult.refreshTokens != null) {
             const nextResponse = NextResponse.next(response);
             await setAuthCookies(nextResponse, authResult.refreshTokens, cookieConfig);
             return nextResponse;
diff --git a/node_modules/@convex-dev/auth/dist/nextjs/server/request.js b/node_modules/@convex-dev/auth/dist/nextjs/server/request.js
index f5a3863..db7d013 100644
--- a/node_modules/@convex-dev/auth/dist/nextjs/server/request.js
+++ b/node_modules/@convex-dev/auth/dist/nextjs/server/request.js
@@ -1,7 +1,8 @@
 import { fetchAction } from "convex/nextjs";
 import { jwtDecode } from "jwt-decode";
 import { NextResponse } from "next/server";
-import { getRequestCookies, getRequestCookiesInMiddleware } from "./cookies.js";
+import { getRequestCookiesInMiddleware } from "./cookies.js";
+import { isLocalHost } from "../../server/utils.js";
 import { getConvexNextjsOptions, isCorsRequest, logVerbose, setAuthCookies, } from "./utils.js";
 export async function handleAuthenticationInRequest(request, options) {
     const verbose = options.verbose ?? false;
@@ -10,8 +11,9 @@ export async function handleAuthenticationInRequest(request, options) {
     const requestUrl = new URL(request.url);
     // Validate CORS
     await validateCors(request);
-    // Refresh tokens if necessary
-    const refreshTokens = await getRefreshedTokens(options);
+    // Refresh tokens if necessary (must read from NextRequest in middleware/proxy;
+    // next/headers cookies() may not reflect the incoming request in Next.js 15+.)
+    const refreshTokens = await getRefreshedTokens(request, options);
     // Handle code exchange for OAuth and magic links via server-side redirect
     const code = requestUrl.searchParams.get("code");
     if (code &&
@@ -22,7 +24,7 @@ export async function handleAuthenticationInRequest(request, options) {
                 ? await options.shouldHandleCode(request)
                 : options.shouldHandleCode))) {
         logVerbose(`Handling code exchange for OAuth or magic link`, verbose);
-        const verifier = (await getRequestCookies()).verifier ?? undefined;
+        const verifier = (await getRequestCookiesInMiddleware(request)).verifier ?? undefined;
         const redirectUrl = new URL(requestUrl);
         redirectUrl.searchParams.delete("code");
         try {
@@ -57,10 +59,46 @@ async function validateCors(request) {
 }
 const REQUIRED_TOKEN_LIFETIME_MS = 60_000; // 1 minute
 const MINIMUM_REQUIRED_TOKEN_LIFETIME_MS = 10_000; // 10 seconds
-async function getRefreshedTokens(options) {
+function readAuthCookiesFromCookieHeader(request) {
+    const header = request.headers.get("cookie");
+    if (!header) {
+        return { token: null, refreshToken: null };
+    }
+    const host = request.headers.get("host") ?? "";
+    const prefix = isLocalHost(host) ? "" : "__Host-";
+    const jwtName = prefix + "__convexAuthJWT";
+    const rtName = prefix + "__convexAuthRefreshToken";
+    let token = null;
+    let refreshToken = null;
+    for (const part of header.split(";")) {
+        const trimmed = part.trim();
+        const eq = trimmed.indexOf("=");
+        if (eq === -1)
+            continue;
+        const name = trimmed.slice(0, eq).trim();
+        let value = trimmed.slice(eq + 1).trim();
+        if (value.startsWith('"') && value.endsWith('"')) {
+            value = value.slice(1, -1);
+        }
+        if (name === jwtName)
+            token = value;
+        if (name === rtName)
+            refreshToken = value;
+    }
+    return { token, refreshToken };
+}
+async function getRefreshedTokens(request, options) {
     const verbose = options.verbose ?? false;
-    const cookies = await getRequestCookies();
-    const { token, refreshToken } = cookies;
+    const cookies = await getRequestCookiesInMiddleware(request);
+    let { token, refreshToken } = cookies;
+    if ((token === null || refreshToken === null) &&
+        request.headers.get("cookie")) {
+        const parsed = readAuthCookiesFromCookieHeader(request);
+        if (token === null && parsed.token !== null)
+            token = parsed.token;
+        if (refreshToken === null && parsed.refreshToken !== null)
+            refreshToken = parsed.refreshToken;
+    }
     if (refreshToken === null && token === null) {
         logVerbose(`No tokens to refresh, returning undefined`, verbose);
         return undefined;
